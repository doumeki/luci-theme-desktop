*.swp
*.swo
*~
.DS_Store
probe/.local-env
# Local ad-hoc probe/debug scripts (kept on disk for local dev, not published).
# The device-agnostic probes are tracked: tests/*.js (the 22 ad-hoc probes) and
# probe/pw-alert-cdp.js — they login through probe/lib.js, which reads
# probe/.local-env (gitignored) or PROBE_* env, so they carry no device secrets.
# What remains below is still machine-specific and stays out of version control.
probe/toast-sync.js
probe/sticky-store-probe.js
probe/window-cascade.js
probe/iface-poll.js
probe/changes-e2e.js
probe/window-height.js
probe/shortcuts-probe.js
probe/runtime-matrix.js
probe/clean-sticky.sh
probe/clean-sticky-keep.sh
probe/bell-badge.js

# Local dev preview pages (kept on disk, not published)
dev.html
mobile-demo.html
